name: Build Windows Executable

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    - name: Install dependencies
      run: npm install --no-package-lock

    - name: Build executable
      run: npx pkg server.js --targets node18-win-x64 --output cottage-printer.exe

    - name: Upload executable
      uses: actions/upload-artifact@v3
      with:
        name: cottage-printer-exe
        path: cottage-printer.exe

    - name: Create Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: Simple Thermal Printer Helper v1.0.${{ github.run_number }}
        body: |
          🖨️ Simple Windows thermal printer helper for restaurant POS systems

          **Features:**
          - HTTP endpoints for kitchen & receipt printing
          - Hybrid Windows + ESC/POS strategy
          - TM-T20III & TM-T88V support
          - POSII integration ready

          **Usage:**
          1. Download cottage-printer.exe
          2. Run on Windows machine with connected Epson printers
          3. Server starts on localhost:3001
          4. Use HTTP endpoints for printing
        files: cottage-printer.exe
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
